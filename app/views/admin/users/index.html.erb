<h1>ユーザ一覧</h1>

<%= link_to "新規登録", new_admin_user_path, class: "btn btn-primary" %>

<table class="table table-hover">
  <thead class="thead-light">
    <tr>
      <th><%= User.human_attribute_name(:id) %></th>
      <th><%= User.human_attribute_name(:name) %></th>
      <th><%= User.human_attribute_name(:email) %></th>
      <th><%= User.human_attribute_name(:created_at) %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.created_at.strftime("%Y年%m月%d日") %></td>
        <td>
          <%= link_to "編集", edit_admin_user_path(user.id), class: "btn btn-primary mr-2" %>
          <%= link_to "削除", admin_user_path(user.id), method: :delete, data: { confirm: "「#{user.name}」#{t("view.task.destroy_confirm")}" }, class: "btn btn-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>